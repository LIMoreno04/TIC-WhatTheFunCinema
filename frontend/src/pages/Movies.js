import { Box, Typography } from "@mui/material";

export default function MoviesPage() {

    return (
        <Box mt={20}>
        <Typography variant="neonPink" fontSize={50}>Peliculas</Typography>
        </Box>
    )


}